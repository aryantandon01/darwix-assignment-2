from django.db import models
# Optional: Add models if you want to store transcriptions, e.g.,
# class Transcription(models.Model):
#     audio_file = models.FileField(upload_to='audio/')
#     result = models.JSONField()
